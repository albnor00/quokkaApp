<!-- circle_shape.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Outer border (black color) -->
    <stroke
        android:width="1dp"
        android:color="#000000" />

    <!-- Inner fill (green color) -->
    <solid android:color="#228B22" />

    <size
        android:width="48dp"
        android:height="48dp" />
</shape>